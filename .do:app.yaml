name: sickoscoop
region: sfo

services:
  - name: backend
    source_dir: /backend
    github:
      repo: devnamekrea/sickoscoop
      branch: main
      deploy_on_push: true
    
    build_command: |
      echo "🚀 SickoScoop Backend Build Process"
      echo "Current directory: $(pwd)"
      echo "Installing backend dependencies..."
      npm install --production=false
      echo "Building frontend from backend directory..."
      cd ../frontend
      npm install --production=false
      npm run build
      echo "Moving frontend build to backend..."
      mv build ../backend/build
      echo "✅ Build complete!"
      cd ../backend
      ls -la build/ || echo "No build directory found"
    
    run_command: node enhanced-server.js
    environment_slug: node-js
    instance_count: 1
    instance_size_slug: basic-xxs
    http_port: 8080
    
    routes:
      - path: /
    
    health_check:
      http_path: /api/health
      initial_delay_seconds: 60
      period_seconds: 10
      timeout_seconds: 5
      success_threshold: 1
      failure_threshold: 3
    
    envs:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "8080"
      - key: MONGODB_URI
        value: mongodb+srv://namekreator-user:fEOzjDe85qkuamrE@cluster0.soprv0r.mongodb.net/sickoscoop?retryWrites=true&w=majority&appName=Cluster0
        type: SECRET
      - key: JWT_SECRET
        value: 4208212352a8353dab72153f1542027785b176c71ea3b4d045035f04ca3caae7718b9ffce777328719170e67e1b642b8b4feb21bf2a7b87b9fbaf3d1588f8270
        type: SECRET
      - key: DO_SPACES_KEY
        value: DO8014CL6J8LU9GJM29B
        type: SECRET
      - key: DO_SPACES_SECRET
        value: NldtL2Fq2H5dzdBB6rD+Cf6xDImRiiFG6gdmIibX5gU
        type: SECRET
      - key: DO_SPACES_BUCKET
        value: sickoscoop-media
        type: SECRET
      - key: DO_SPACES_REGION
        value: sfo2
      - key: DO_SPACES_ENDPOINT
        value: sfo2.digitaloceanspaces.com
      - key: FRONTEND_URL
        value: ${APP_DOMAIN}
      - key: TRACKING_DOMAIN
        value: ${APP_DOMAIN}
      - key: TRACKING_URL
        value: https://${APP_DOMAIN}

alerts:
  - rule: DEPLOYMENT_FAILED
  - rule: DOMAIN_FAILED